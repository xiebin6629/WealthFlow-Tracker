name = "wealthflow-price-api"
main = "src/index.ts"
compatibility_date = "2024-06-01"

# 开发环境配置
[dev]
port = 8787

# 生产环境会自动使用 workers.dev 域名
# 部署后 URL 格式: https://wealthflow-price-api.<your-subdomain>.workers.dev
